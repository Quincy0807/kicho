import shell from 'shelljs';
console.log(shell.exec('ls', '/Users/Quincy'));
const ShellInterface = React.createClass({
  render() {
    return <div>Test</div>;
  },

});

export default ShellInterface;
